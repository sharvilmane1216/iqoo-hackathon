# Template for developer-local secrets. Copy to `local.properties`
# (which is gitignored) and fill in a real key:
#
#   cp local.defaults.properties local.properties
#
# Never commit real keys. The app reads this value via BuildConfig.
CALLMISSED_API_KEY=placeholder
