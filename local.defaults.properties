# Dummy key so the project builds. Cloud talk / STT / TTS / search /
# medicine naming stay off until you replace this with a real key.
#
# Get a key: https://console.callmissed.com  (llm, stt, tts, search)
#   cp local.defaults.properties local.properties
# Then set CALLMISSED_API_KEY=cm_... and rebuild.
#
# Never commit a real key in local.properties.
CALLMISSED_API_KEY=cm_dummy_get_your_key_from_callmissed
