# Required. Copy to `local.properties` (gitignored) and replace placeholder
# with a real CallMissed key from https://console.callmissed.com
# (permissions: llm, stt, tts, search).
#
#   cp local.defaults.properties local.properties
#
# assembleDebug / Run fail until this is a real cm_... key.
# Never commit local.properties.
CALLMISSED_API_KEY=placeholder
