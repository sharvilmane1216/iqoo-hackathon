<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Requested + rationale shown by app/ onboarding. tools/ only *uses* them.
         CALL_PHONE denied  -> ACTION_DIAL fallback (ContactTools/SosTools).
         SEND_SMS denied     -> spoken non-technical message, nothing sent.
         SCHEDULE_EXACT_ALARM denied (common on Android 14) -> inexact alarm
           + Room record + spoken note (ReminderTools).
         POST_NOTIFICATIONS denied (Android 13+) -> reminder fires through
           the TTS hook only, no status-bar notification.
         CAMERA: setTorchMode needs it on some OEMs (SystemTools flashlight).
         LOCATION: only used to append text to SOS SMS (SosTools). -->
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application>
        <receiver
            android:name=".ReminderReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.aasra.tools.REMINDER_FIRED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".ReminderRescheduleReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Notification access CANNOT be granted at runtime. app/ onboarding
             must route the user to Settings via NotificationAccess.settingsIntent().
             See NotificationReader.kt. -->
        <service
            android:name=".AasraNotificationListener"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
    </application>
</manifest>
